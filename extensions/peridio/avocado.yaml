supported_targets: '*'

distro:
  release: 2024
  channel: edge

extensions:
  avocado-ext-peridio:
    version: 2024.1.0
    release: r0
    summary: Peridio firmware updater
    description: Peridio firmware updater
    license: Apache-2.0
    url: https://github.com/avocadolinux/avocado-os
    vendor: Avocado Linux <info@avocadolinux.org>

    overlay: overlay

    enable_services:
      - peridiod.service

    on_merge:
      - systemctl restart --no-block peridiod.service

    packages:
      peridiod: '*'

sdk:
  image: docker.io/avocadolinux/sdk:{{ avocado.distro.release }}-{{ avocado.distro.channel }}
