# NemoClaw persistent state directories on /var (R/W partition)
d /var/lib/nemoclaw 0755 root root -

# Ollama model storage on /var (R/W partition)
d /var/lib/ollama 0755 root root -
d /var/lib/ollama/models 0755 root root -
