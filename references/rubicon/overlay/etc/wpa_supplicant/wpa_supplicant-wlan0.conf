# WiFi Configuration
# wpa_supplicant configuration for wlan0

ctrl_interface=/run/wpa_supplicant
ctrl_interface_group=wheel
update_config=1

network={
    ssid=""
    # Generate with: wpa_passphrase "your network" your_password
    # Use the 64-char hex string (no quotes) for the hashed PSK:
    psk=
    key_mgmt=WPA-PSK
}

